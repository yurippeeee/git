git add -p ファイル名 -> ハンク(変更されていない行に挟まれている１つの変更箇所)を選択的にaddする。バグAの修正なのに、バグBの修正なども一緒にコミットすると分かりづらいので、そういうときに使用する。
　y -> インデックスに追加(以降、ステージ)するとき
　n -> ステージしないとき
　q -> この場でgit addの動作を終了する
　s -> splitの意味。ハンクを分割したいときに使用
　e -> エディットモードになり、ハンク内の指定した領域のみをaddする
