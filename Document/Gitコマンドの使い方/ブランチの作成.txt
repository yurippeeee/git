git branch br-a -> 現在のHEADの位置に新しいブランチ(br-a)を作成(HEADは元の場所を指したまま)
git checkout -b br-b-> 現在のHEADの位置に新しいブランチ(br-b)を作成(HEADは新しいブランチを指す)(バグfixをするとき、緊急パッチを当てるときなどに使う)
　コマンドの後ろにコミットIDをつけると、現在のHEADではなく、そのコミットの位置にブランチを作成する
