git checkout main -> HEADをmainというブランチもしくはコミットに設定し、ワークツリーとインデックスをそのHEADの内容に更新する。
　コミットを指定するとDetached HEADとなる
　ようするにHEADラベルの位置を操作するコマンド

ブランチ名・タグ以外に、コミットIDでも指定可能
Detached HEADの状態では、コミットすることができない(最新の状態ではないため)。なので調査用として使う。
