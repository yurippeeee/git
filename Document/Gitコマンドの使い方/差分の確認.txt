git diff コミットA コミットB -> コミットAからコミットBへの差分を見る(逆にすると差分も逆になる)(ハッシュされているコミットIDで指定し、IDはダブリがない部分までの記述で良い)
git diff -> インデックスからワークツリーへの差分を見る(コミット前に変更を確認したいときなどに使用)
　ワークツリー -> 作業中のディレクトリ
　インデックス -> addで追加される領域
　リポジトリ -> コミットで追加される領域

git diff --cached -> インデックスからリポジトリへの差分を見る
