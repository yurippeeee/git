git reset
　--soft -> ワークツリーとインデックスは変更せずに、HEADを指定したコミットにリセット(もとに戻すのが大変)
　--mixed -> HEADのインデックスを指定したコミットにリセット(現在のgit addの結果が無効化される)(デフォルト)
　--hard -> HEADのインデックスとワークツリーを指定したコミットにリセット(開発の過程でうまく動かなくなったりしたら、これをすればきれいな状態に戻る)
